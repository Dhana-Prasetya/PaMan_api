const { PrismaClient } = require("@prisma/client");
const prisma = new PrismaClient();

const userAdressControllers = {
	InsertUserAddress: async (req, res) => {},
};

module.exports = userAdressControllers;
